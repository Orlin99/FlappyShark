package org.example;
import javax.swing.*;
public class BackgroundForInstructionsPanel extends JLabel {
    public BackgroundForInstructionsPanel(Icon image) {
        super(image);
        this.setSize(525,550);
    }
}
